"use client";
import { useEffect,useState } from "react";
import Script from "next/script";
import Link from "next/link";
import "./CookieConsent.css";
export default function CookieConsent(){const[choice,setChoice]=useState<string|null>(null);useEffect(()=>setChoice(localStorage.getItem('myl3d-cookie-consent')),[]);function save(v:string){localStorage.setItem('myl3d-cookie-consent',v);setChoice(v)}return <>{choice==='accepted'&&<><Script strategy="afterInteractive" src="https://www.googletagmanager.com/gtag/js?id=G-QP5HESG7SP"/><Script id="ga-consented" strategy="afterInteractive">{`window.dataLayer=window.dataLayer||[];function gtag(){dataLayer.push(arguments)}gtag('js',new Date());gtag('config','G-QP5HESG7SP',{anonymize_ip:true});`}</Script></>}{choice===null&&<section className="cookie-banner" aria-label="Gestión del consentimiento de cookies"><div><strong>Tu privacidad</strong><p>Usamos cookies técnicas necesarias y, solo si aceptas, cookies de medición para conocer el uso de la web. Consulta la <Link href="/cookies">política de cookies</Link>.</p></div><div className="cookie-banner__actions"><button onClick={()=>save('rejected')}>Rechazar opcionales</button><button className="cookie-accept" onClick={()=>save('accepted')}>Aceptar</button></div></section>}</>}
